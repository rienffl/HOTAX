# HOTAX — 2025. 진해용원고 2-3 국세청

모노레포 구성: `server`(Express API) + `web`(Next.js 14). DB는 Supabase(Postgres).
배포: server→Render, web→Vercel. DB 스키마는 `server/supabase/schema.sql`.

관리자 계정(Seed):
- 아이디: rienffl
- 비밀번호: Inwoo5867^
